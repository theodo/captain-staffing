// @flow

import styled from 'styled-components';

export const StyledLeftBarUser = styled.div`
  height: ${props => props.height}px;
  min-height: 40px;
  padding: 5px;
  display: flex;
  position: relative;
  box-shadow: inset 0 1px 0 0 #eee;
`;
