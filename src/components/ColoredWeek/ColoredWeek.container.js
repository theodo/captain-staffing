// @flow

import { connect } from 'react-redux';
import ColoredWeek from './ColoredWeek.component';

const mapStateToProps = () => ({});

const mapDispatchToProps = () => ({});

export default connect(
  mapStateToProps,
  mapDispatchToProps,
)(ColoredWeek);
